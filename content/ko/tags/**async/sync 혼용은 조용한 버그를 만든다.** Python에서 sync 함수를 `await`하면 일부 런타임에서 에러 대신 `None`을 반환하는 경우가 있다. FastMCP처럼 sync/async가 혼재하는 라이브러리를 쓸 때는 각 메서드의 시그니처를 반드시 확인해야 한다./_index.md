---
title: "**async/sync 혼용은 조용한 버그를 만든다.** Python에서 sync 함수를 `await`하면 일부 런타임에서 에러 대신 `None`을 반환하는 경우가 있다. FastMCP처럼 sync/async가 혼재하는 라이브러리를 쓸 때는 각 메서드의 시그니처를 반드시 확인해야 한다."
image: "/images/taxonomy/tag-**async/sync 혼용은 조용한 버그를 만든다.** Python에서 sync 함수를 `await`하면 일부 런타임에서 에러 대신 `None`을 반환하는 경우가 있다. FastMCP처럼 sync/async가 혼재하는 라이브러리를 쓸 때는 각 메서드의 시그니처를 반드시 확인해야 한다..svg"
---
