---
title: "**실행 중인 프로세스**: Claude Code 세션, `npm run dev` 같은 라이브 프로세스는 복원되지 않는다. 이것은 tmux와의 핵심 차이점이다 — tmux는 서버가 살아있는 한 세션이 유지되지만, cmux는 프로세스를 다시 시작해야 한다."
image: "/images/taxonomy/tag-**실행 중인 프로세스**: Claude Code 세션, `npm run dev` 같은 라이브 프로세스는 복원되지 않는다. 이것은 tmux와의 핵심 차이점이다 — tmux는 서버가 살아있는 한 세션이 유지되지만, cmux는 프로세스를 다시 시작해야 한다..svg"
---
